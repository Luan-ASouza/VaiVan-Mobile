<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_chat"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/beige"
    android:orientation="vertical"> <!-- BARRA DE TOPO -->
    <LinearLayout
        android:id="@+id/barraTopo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/dark_gray"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:padding="12dp">

        <TextView
            android:id="@+id/txtNomeContato"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Conversa"
            android:textColor="#FFFFFF"
            android:textSize="20sp"
            android:textStyle="bold" />
    </LinearLayout> <!-- LISTA DE MENSAGENS -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerMensagens"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_margin="10dp"
        android:layout_weight="1"
        android:background="@drawable/bg_withe_round_10"
        android:clipToPadding="false"
        android:paddingTop="8dp"
        android:paddingBottom="8dp" /> <!-- BARRA DE DIGITAR -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/barraMensagem"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/beige"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:padding="8dp">

        <EditText
            android:id="@+id/edtMensagem"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_white_rounded"
            android:hint="Digite sua mensagem..."
            android:inputType="textMultiLine"
            android:maxLines="4"
            android:padding="10dp"
            android:textColorHint="@color/light_gray"
            android:textColor="@color/dark_gray"
            android:layout_marginEnd="10dp"

            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/btnEnviar"
            app:layout_constraintHorizontal_weight="1"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />


        <!-- BOTÃO REDONDO (Proporção 1:1 baseada na altura) -->
        <ImageButton
            android:id="@+id/btnEnviar"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:background="@drawable/bg_button_darkgrey"
            android:contentDescription="Enviar mensagem"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_enviar_mensagem"
            android:padding="5dp"
            android:paddingStart="7dp"

            app:layout_constraintBottom_toBottomOf="@+id/edtMensagem"
            app:layout_constraintDimensionRatio="1:1"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@+id/edtMensagem" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</LinearLayout>